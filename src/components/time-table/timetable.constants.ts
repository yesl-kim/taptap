export const STEP = 30 // 시간표 간격 (분)

export const HEIGHT_PER_HOUR = 48
export const HEIGHT_PER_MINUTE = HEIGHT_PER_HOUR / 60
export const TOTAL_HEIGHT = HEIGHT_PER_HOUR * 24
export const HEIGHT_PER_STEP = HEIGHT_PER_MINUTE * STEP
